{{- if ne .chezmoi.os "windows" -}}
#!/bin/bash

# Brewfile hash: {{ include "Brewfile.tmpl" | sha256sum }}

if command -v brew &> /dev/null; then
    echo "Running brew bundle..."
    # テンプレート(tmpl)ではなく、反映後の実ファイルを読み込むように修正
    brew bundle --file=~/Brewfile
fi
{{- end -}}
